package tester;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.Iterator;
import java.util.LinkedList;
import java.util.List;
import java.util.ListIterator;

import core.Employee;

public class TestCollection {

	public static void main(String[] args) 
	{
		/*ArrayList<Employee> emps=new ArrayList<>();
		emps.add(new Employee(1, "pranaw", 1000));
		Collections.sort(numbers);
		//Arrays.sort(numbers.toArray());
		System.out.println(numbers);
		
		System.out.println("Arraylist of nums :");
		
		List<Number> list=Arrays.asList(1,2,3);
		
		Iterator<Integer> iterator=numbers.iterator();
		ListIterator<Integer> literator=numbers.listIterator(numbers.size());
		
		while(iterator.hasNext())
		{
			numbers.add(55);
			
			numbers.size();
			System.out.println(iterator.next());
			
		}
		
		
		while(iterator.has)
		{
			numbers.add(55);
			
			numbers.size();
			System.out.println(iterator.next());
			
		}
		System.out.println("reverse");
		while(literator.hasPrevious())
			System.out.println(literator.previous());
		System.out.println(iterator.next());
		System.out.println("again");
		while(iterator.hasNext())
			System.out.println(iterator.next());
		
		//numbers.forEach(System.out::println);
		
		LinkedList<Integer> nums=new LinkedList<>();
		nums.add(1);
		nums.add(4);
		nums.add(22);
		nums.add(24);
		nums.add(12);
		nums.add(9);
		System.out.println("Listlist of nums :");
		
		ListIterator<Integer> l=nums.listIterator(nums.size());
		while(l.hasPrevious())
			System.out.println(l.previous());
	//	nums.forEach(System.out::println);
		*/
		
	}
	 
}
